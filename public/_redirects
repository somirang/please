# https://docs.netlify.com/routing/redirects/#syntax-for-the-redirects-file
# netlify에서 react router가 하는 route를 인식하지 못해서,
# react router가 동작하기 전까지는 페이지를 찾을 수 없다는 에러를 띄웁니다.
# 이러한 버그를 막기 위해 사용자들이 접근할 수 있는 몇몇 url들에 대해 강제로 redirect rules를 추가합니다.

# Redirects from what the browser requests to what we serve
# /to                   /from
/*                      /index.html   200
